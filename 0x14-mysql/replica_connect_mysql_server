#CHANGE MASTER TO" a MySQL command used to configure the replication settings 
#for a replica (slave) server in a MySQL replication setup.

CHANGE MASTER TO MASTER_HOST='web-01', MASTER_USER='replication_user', MASTER_PASSWORD='replication_password', MASTER_LOG_FILE='mysql-bin.XXXXXX', MASTER_LOG_POS=XXXXXX;

#use show master status to get details and get replication_password you set.
